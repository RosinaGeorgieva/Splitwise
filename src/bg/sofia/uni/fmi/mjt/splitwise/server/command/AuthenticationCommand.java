package bg.sofia.uni.fmi.mjt.splitwise.server.command;

public abstract class AuthenticationCommand extends ExecutableCommand {
    public AuthenticationCommand(Command command) {
        super(command);
    }
}
